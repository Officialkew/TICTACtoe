for (int i = 0; i < 3; i++) {
      for (int j = 0; j < 3; j++) {
        board.getSquare(i, j).getState2().set(Square.State.CROSS);
        System.out.printf("Location: %d,%d State: %s\n", i, j, board.getSquare(i, j).getState());